## 1. 문제 분석

이 문제는 $N \times N$ 격자에서 주 대각선, 가운데 행, 부 대각선, 가운데 열이라는 4개의 주요 선을 지정된 각도만큼 회전시키는 시뮬레이션 문제이다. 
각도 $D$가 양수이면 시계 방향으로, 음수이면 반시계 방향으로 45도씩 회전하며 그 외의 요소들은 위치를 유지해야 한다.

**핵심 로직 (3단계):**

1. **회전 횟수 산출:** 주어진 각도 $D$를 45로 나누어 절댓값만큼의 회전 횟수를 결정한다. $D$의 부호에 따라 시계 방향 혹은 반시계 방향 함수를 선택한다.
    
2. **45도 회전 규칙 적용:**
    
    - **시계 방향:** 주 대각선 $\rightarrow$ 가운데 열, 가운데 열 $\rightarrow$ 부 대각선, 부 대각선 $\rightarrow$ 가운데 행, 가운데 행 $\rightarrow$ 주 대각선 순으로 원소를 이동시킨다.
        
    - **반시계 방향:** 시계 방향의 역순으로 원소를 이동시키며 각 라인의 인덱스 대응 관계를 정확히 매핑한다.
        
3. **배열 상태 동기화:** 회전 시 원본 값이 덮어씌워지는 것을 방지하기 위해 임시 배열(`nmap`)에 먼저 기록한 후, 한 회차가 끝날 때마다 원본 배열(`map`)을 업데이트한다.
    

## 2. 배운 점

**(1) 인덱스 방향성의 정밀한 제어:** 부 대각선($i, N-1-i$)과 관련된 데이터를 가운데 행이나 열로 옮길 때, 인덱스의 증감 방향을 세밀하게 따져야 한다. 단순히 변수 $i$를 공유하는 것이 아니라, 어떤 축이 고정되고 어떤 축이 변하는지 파악하는 것이 중요하다.

**(2) 0-based 인덱스 범위의 정확성:** 배열의 크기가 $N$일 때 마지막 원소의 인덱스는 $N-1$이다. 인덱스 계산 시 $N-i$를 사용하는 실수는 잘못된 메모리 접근이이나 잘못된 위치 계산을 유발하므로 항상 $N-1-i$ 형태를 유지해야 한다.

**잘못 생각한 부분:** 부 대각선에서 가운데 열이나 행으로 가는 것이랑 가운데 열이나 행에서 부 대각선으로 갈 때에 부 대각선의 index 변화가 다른데 아무생각 없이 동일하게 코드를 구현해서 잘못된 값이 나왔다. 또한 반복문 경계값 설정 시 $N-1$을 고려하지 않아 결과값에 계속해서 0이 포함되어 나왔었다.

## 3. 최종 코드 및 동작 방식 정리

전체 테스트 케이스($T$)에 대해 배열을 입력받고 회전 함수를 실행한다. 각 함수는 45도 회전을 1회 수행하는 로직을 cnt만큼 반복한다.

**작동 방식:**

1. **초기화 및 입력:** 매 테스트 케이스마다 배열을 입력받고 임시 배열을 준비한다.
    
2. **방향 제어:** $D$가 음수일 경우 반시계 방향 함수를, 양수일 경우 시계 방향 함수를 호출한다.
    
3. **라인 이동:**
    
    - `mid = (N-1)/2`를 기준으로 가운데 행과 열을 특정한다.
        
    - 주 대각선(`i, i`), 부 대각선(`i, N-1-i`), 가운데 행(`mid, i`), 가운데 열(`i, mid`) 사이의 값을 규칙에 따라 교체한다.
        
4. **결과 출력:** 모든 회전이 완료된 `map`을 행 단위로 출력한다.
    

```c++
#include<iostream>
using namespace std;

#define MAX (501)
int T,N,D,cnt; // D>0 시계, D<0 반시계, 횟수: |D|/45
int map[MAX][MAX];
int nmap[MAX][MAX];

// X를 시계 방향으로 45도 돌리면
// 주 대각선((1,1),(2,2)...(n,n)) -> ((n+1)/2번째 열)로 이동
// 가운데 열 -> 부 대각선((n,1),(n-1,2)...(1,n))로 이동
// 부 대각선 -> 가운데 행 (n+1)/2번째
// 가운데 행 -> 주 대각선
// 원본 순서 유지
// 다른 원소 위치는 그대로
// 1,1 -> 1,(n+1)/2 2,2 2,(n+1)
void clockwise45(){
    while(cnt--){
        // 원본 -> 카피
        for(int i=0; i<N; i++){
            for(int j=0; j<N; j++){
                nmap[i][j] = map[i][j];
            }
        }
        
        int mid = (N-1)/2;
        for(int i=0; i<N; i++){
            nmap[i][mid] = map[i][i];// 주 대각선 -> 가운데 열 
        }   
        for(int i=0; i<N; i++){
            nmap[i][N-1-i] = map[i][mid];// 가운데 열 -> 부 대각선
        }
        for(int i=0; i<N; i++){
            nmap[mid][i] = map[N-1-i][i];// 부 대각선 -> 가운데 행
        }
        for(int i=0; i<N; i++){
            nmap[i][i] = map[mid][i];// 가운데 행 -> 주 대각선
        }
        
        // 결과 -> 원본
        for(int i=0; i<N; i++){
            for(int j=0; j<N; j++){
                map[i][j] = nmap[i][j];
            }
        }
    }
}

// X를 시계 반대방향으로 45도 돌리면
// 주 대각선 -> 가운데 행
// 가운데 행 -> 부 대각선
// 부 대각선 -> 가운데 열
// 가운데 열 -> 주 대각선
// 원본 순서 유지
// 다른 원소 위치는 그대로
void counterclockwise45(){
    while(cnt--){
        // 원본 -> 카피
        for(int i=0; i<N; i++){
            for(int j=0; j<N; j++){
                nmap[i][j] = map[i][j];
            }
        }
        
        int mid = (N-1)/2;
        for(int i=0; i<N; i++){
            nmap[mid][i] = map[i][i];// 주 대각선 -> 가운데 행 
        }   
        for(int i=0; i<N; i++){
            nmap[N-1-i][i] = map[mid][i];// 가운데 행 -> 부 대각선
        }
        for(int i=0; i<N; i++){
            nmap[i][mid] = map[i][N-1-i];// 부 대각선 -> 가운데 열
        }
        for(int i=0; i<N; i++){
            nmap[i][i] = map[i][mid];// 가운데 열 -> 주 대각선
        }

        // 결과 -> 원본
        for(int i=0; i<N; i++){
            for(int j=0; j<N; j++){
                map[i][j] = nmap[i][j];
            }
        }
    }
}

int main(void){
    ios_base :: sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    cin >> T;
    
    for(int p=0; p<T; p++){
        cin >> N >> D;
        // 초기화
        for(int i=0; i<N; i++){
            for(int j=0; j<N; j++){
                map[i][j]=0;
                nmap[i][j]=0;
            }
        }
        // input
        for(int i=0; i<N; i++){
            for(int j=0; j<N; j++){
                cin >> map[i][j];
            }
        }
        // 동작
        if(D<0){
            cnt = (-D)/45;
            counterclockwise45();
        } 
        else{
            cnt = D/45;
            clockwise45();
        } 
        // output
        for(int i=0; i<N; i++){
            for(int j=0; j<N; j++){
                cout << map[i][j] << ' ';
            }
            cout << '\n';
        }
    }
    return 0;
}
```